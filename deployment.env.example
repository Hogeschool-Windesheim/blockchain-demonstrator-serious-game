# Blockchain Demonstrator Deployment Configuration
# Copy this file to deployment.env and customize for your server

# ASP.NET Core Environment (Production/Development)
ASPNETCORE_ENVIRONMENT=Production

# Public-facing API URL that browsers will use to connect
# This should be the HTTPS domain accessible from the internet
PUBLIC_API_URL=https://your-domain.com

# CORS allowed origins (comma-separated list)
# Include all domains that will access the API
CORS_ORIGINS=https://your-domain.com,http://your-server-ip:5000

# .NET Core ICU version override (required for some Linux distributions)
CLR_ICU_VERSION_OVERRIDE=70

# Disable .NET telemetry
DOTNET_PRINT_TELEMETRY_MESSAGE=false

# Server IP for internal communication
# This will be passed as a command-line argument to the applications
SERVER_IP=your-server-ip:port

# Example configuration for demonstrator.valuechainhackers.xyz:
# PUBLIC_API_URL=https://demonstrator.valuechainhackers.xyz
# CORS_ORIGINS=https://demonstrator.valuechainhackers.xyz,http://10.0.5.13:5000
# SERVER_IP=10.0.5.13:8080
